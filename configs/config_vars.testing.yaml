worker-threads: 4
worker-fs-threads: 2
worker-grpc-threads: 2
logger-level: debug

is-testing: true

server-port: 8082
server-grpc-port: 8081

secdist-path: /secure_data.json

# authorization_db-1 is the service name + _ + filename of the db_1.sql
dbconnection: 'postgresql://testsuite@localhost:15433/authorization_db_1'

client-user-service-endpoint: 'user_service:8081'
client-user-service-deadline: '2s'
access-token-ttl: '1h'